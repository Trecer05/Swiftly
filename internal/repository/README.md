# Data Access Layer

Работа с хранилищами данных.

## Подпакеты:
- `postgres/` - Реализации для PostgreSQL
- `cache/` - Redis-кэш

## Принципы:
- Каждая сущность имеет свой репозиторий
- Изолирует БД от бизнес-логики

Пример:
```go
user, err := userRepo.GetByID(ctx, "123")
```